# Zadání R11 – Sales: účtenka, detail nákupu, hromadný tisk palubních vstupenek

Repozitář: `martinhoricky-bdo/rewrite-cobol`. Větev **`codex/R11-receipt`** z aktuální **`rewrite`** (R01–R10). PR do **`rewrite`**.

## Kontext

- `AGENTS.md`. Závazné.
- `docs/rewrite/02-functional-spec.md`: **UC-S08**, **UC-S09**, obrazovka **S-SALE-DONE**. Závazné.
- Legacy: `CICS/SALES-MAP/RECEIPT-COB` (`PRINTPA`), vzor `RECEIPT-FORMAT`.

## Rozsah

### 1. Views (`sales/urls.py`), role `sales`, `ceo`

| URL | Name | Obsah |
|---|---|---|
| `GET /sales/buys/<int:buyid>/receipt/` | `sales:receipt` | tisková stránka (bez `base.html`, `print.css`), obsah dle `RECEIPT-FORMAT` v pořadí: `RECEIPT`; `BUYID: {buyid}`; `CB/CS/CH` a pod tím `Payment: not recorded`; `Le {DD/MM/YYYY} a {HH:MM:SS}` (buydate/buytime); `COBOL AIRLINES`; `PARIS`; `75000`; `**********`; oddělovač `------------------------------`; `MONTANT = {price} EUR` (formát `362.97`); `DEBIT/CREDIT`; `TICKET CLIENT`; `TO KEEP`. Tlačítko `Print` (skryté při tisku), odkaz `Back`. |
| `GET /sales/buys/<int:buyid>/boarding-passes/` | `sales:boarding_passes` | všechny letenky nákupu, každá jako blok stejného vzhledu jako R07 (znovu použij šablonu/partial `sales/_boarding_pass.html` a `boarding_pass_context`), `page-break-after: always` mezi nimi |

- V `buy_detail` (R10) nahraď placeholder odkazy `{% url %}` a doplň tlačítko `Print all boarding passes`.
- 404 pro neexistující nákup.
- Refaktor R07: vytáhni obsah palubní vstupenky do partialu `sales/_boarding_pass.html`, aby ho používaly obě stránky (jednotlivá i hromadná).

### 2. Testy

- `tests/views/test_receipt.py`: obsah pro nákup 3 × 120.99 (`MONTANT = 362.97 EUR`, `BUYID`, `Payment: not recorded`, datum `DD/MM/YYYY`); 404; oprávnění (`sales`, `ceo` 200; `hr` 403); stránka neobsahuje menu.
- `tests/views/test_boarding_passes.py`: nákup se 3 letenkami → 3 bloky (3× `BOARDING PASS`), každý se správným jménem a sedadlem; jednotlivá vstupenka (R07) stále funguje.

## Akceptační kritéria

1. Se seedem po prodeji z R10: `/sales/buys/2/receipt/` a `/sales/buys/2/boarding-passes/` fungují a tisknou se čitelně.
2. `make check` zelený.
3. Změny jen v `app/` a CHANGELOG.

## PR

Větev `codex/R11-receipt`, cíl `rewrite`, název `R11: Sales – účtenka a hromadný tisk`, popis dle šablony, commit prefix `R11:`.
